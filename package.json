{
  "name": "multiprojectstore",
  "version": "0.3.0",
  "description": "Harness-agnostic fork of ProjectStore: one vault, one CLI, the same agent roles in Claude Code, Codex and OpenCode.",
  "license": "MIT",
  "type": "module",
  "bin": { "mps": "bin/mps" },
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test tests/*.test.mjs" },
  "files": ["bin", "scripts", "agents", "prompts", "skills", "scaffold", "templates", "docs", "AGENTS.md", "README.md"]
}
